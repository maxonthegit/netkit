Ubuntu 14.04 (trusty) x86_64


Additional packages required for kernel64: none
Additional packages required for kernel32: gcc-multilib


Additional packages required for filesystem64: libssl-dev (for ovs; also applies to filesystem32)
Additional packages required for filesystem32: debootstrap fakeroot dpkg-dev +adding user to the 'disk' group

